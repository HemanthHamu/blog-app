<!DOCTYPE html>
<html lang="en">
  <head>
   <title>Register</title>
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
      body {
        background-color: #171616;
        font-family: "Lucida Sans", "Lucida Sans Regular", "Lucida Grande",
          "Lucida Sans Unicode", Geneva, Verdana, sans-serif;
      }
      .container {
        width: 400px;
        margin: 50px auto;
        background-color: #2c2a2a;
        color: #fff;
        padding: 40px;
        border-radius: 10px;
      }
      .container h1 {
        text-align: center;
      }
      .container form {
        display: flex;
        flex-direction: column;
        justify-content: start;
        gap: 20px;
      }
      .container label {
        font-size: 20px;
      }
      .container input {
        border: none;
        border-bottom: 2px solid #4b4a4a;
        outline: none;
        padding: 8px;
        background-color: #2c2a2a;
        color: #d2e3c8;
        font-size: 21px;
        margin-bottom: 30px;
      }
      input:focus {
        border-bottom: 3px solid #605d5d;
      }
      ::placeholder {
        font-size: 18px;
        color: #fff;
        opacity: 0.2;
      }
      .container a {
      
        text-decoration: none;
        color: #d2e3c8;
      }
      .container button {
        padding: 8px;
        background-color: #4f4e4e;
        border: none;
        font-size: 19px;
        color: #d2e3c8;
        border-radius: 10px;
      }
      button:hover {
        background-color: #454444;
      }
      @media (max-width:768px) {
        .container{
          max-width: 100%;
          width: 75%;
        }
      }
    </style>
  </head>
<body>
      <div class="container">
      <h1>Registration Form</h1>
        <form action="/submit" method="post">
            <label for="email">Email:</label>
            <input  type="email" id="email" name="email" placeholder="Enter your email..." required/>
            <label for="password">Password:</label>
            <input  type="password" id="password" name="password" placeholder="Enter your password" minlength="6" maxlength="20" required/>
            <label for="confirmpassword">Confirm Password:</label>
            <input  type="text"  id="confirmpassword" name="confirmpassword" placeholder="Confirm password"required/>
            <% if (error) { %>
              <p style="background-color: #052609; color: red; margin-top: -20px; padding: 10px;  " >
              <%= error %>
              </p>
              <% } %>
            <a href="/login1">already have an account? login here</a>
            <button type="submit">Register</button>
        </form>
      </div>
</body>
</html>
